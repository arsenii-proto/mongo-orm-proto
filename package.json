{
  "name": "mongo-orm-proto",
  "version": "1.0.0",
  "description": "MongoDB ORM for JS",
  "main": "index.js",
  "scripts": {
    "test": "mocha --require ./test/base.js --recursive ./test",
    "test:u": "mocha --require ./test/base.js --recursive ./test/unit",
    "test:i": "mocha --require ./test/base.js --recursive ./test/int",
    "test:w": "mocha --watch  --require ./test/base.js --recursive ./test",
    "test:wu": "mocha --watch  --require ./test/base.js --recursive ./test/unit",
    "test:wi": "mocha --watch  --require ./test/base.js --recursive ./test/int",
    "test-coverage": "istanbul cover _mocha —- -R spec"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/arsenii-proto/mongo-orm-proto.git"
  },
  "keywords": [
    "ORM",
    "mongo",
    "mongodb",
    "database",
    "schema",
    "nested-documents"
  ],
  "author": "Arsenii Proto",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/arsenii-proto/mongo-orm-proto/issues"
  },
  "homepage": "https://github.com/arsenii-proto/mongo-orm-proto#readme",
  "dependencies": {
    "assert": "^1.4.1",
    "babel-eslint": "^10.0.1",
    "eslint": "^5.9.0",
    "eslint-config-prettier": "^3.3.0",
    "mongodb": "^3.1.8",
    "mongoose": "^5.3.12",
    "ramda": "^0.25.0",
    "sinon": "^7.1.1"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "istanbul": "^0.4.5",
    "jsdoc": "^3.5.5",
    "mocha": "^5.2.0",
    "supertest": "^3.3.0"
  }
}
